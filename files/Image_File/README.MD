<!--fused:filePreview-->
Extensions: `png` `jpg` `jpeg` `gif` `webp` `avif` `bmp` `ico` `svg`
Menu order: 1

<!--fused:readme-->
Show an image file at its natural size, in any format the browser decodes — including SVG, ICO and AVIF, which ImageIO cannot read. Use ImageIO_File instead when you want the pixels as an array on the map.
